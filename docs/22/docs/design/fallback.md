# フォールバックの実装とビルド戦略

割注においてフォールバック用アルゴリズムを実装する際の最適な方法論とそのビルド戦略について記す。

## 背景

割注の分割位置算出には複数の戦略（指定位置分割、単語分割、中間分割）が存在する。これらを順次試行し、有効な値が得られなければ次の戦略へ移行する「フォールバック」の仕組みが必要である。しかし、これを割注の実装をする関数内において `for` や `if` などの制御文でベタ書きすると、ビジネスロジック以外のノイズとなり、コードの保守性が著しく低下する。また、最後の戦略は必ず成功するという論理的保証をコード上で表現しようとすると、到達不能な例外処理を記述せざるを得ないという矛盾が生じる。

## 目的

本ドキュメントは、これらの課題を解決するために「制御フローの抽象化」を行い、宣言的な記述を可能にする設計戦略を記したものである。また、抽象化による実行効率の低下というデメリットに対し、ビルドプロセス（AST置換）を活用して「保守性」と「実行性能」を両立させるための戦略についても詳述する。

## 目次

1. [背景と課題][]
    1. [割注における分割位置決定方法][]
    2. [フォールバックのアルゴリズム][]
        1. [`if-else if-else`][]
        2. [関数配列][]
        3. [最後の関数だけは確実に実行する][]
    3. [責任の混在][]
2. [責任分離とトレードオフ][]
    1. [長所][]
    2. [短所][]
3. [API実装パターン網羅][]
    1. [戻り値のパターン][]
        1. [分割後テキスト][]
            1. [`[line1, line2]`][]
        2. [分割位置(index,skip)][]
            1. [`[index, skip]`][]
            2. [`{index, skip}`][]
    2. [メソッドのパターン][]
        1. [命令型パターン][]
            1. [ループなし][]
                1. [skipハードコーディング][]
                2. [skip代入1回のみ][]
                3. [分割メタ文字字数カウント][]
            2. [ループあり][]
                1. [`if-else if-else`][]
                2. [全件走査・例外あり][]
                3. [最後のみ強制実行][]
        2. [関数配列による抽象化パターン（fallback関数）][]
            1. [JS型][]
            2. [TS型][]
            3. [責任混在型][]
            4. [ラッパー型][]
            5. [委譲型][]
        3.[Strategiesクラスパターン][]
            1. [引数受付（run(i)方式）][]
            2. [イテレータ（next()方式）][]
        4. [抽象化パターン][]
            1. [親クラスによる分類][]
            2. [メソッドチェーン][]
        5. [具象化パターン（WarichuSplitter）][]
            1. [nullで失格を表現する][]
            2. [Symbolで失格を表現する][]
            3. [オプションをコンストラクタ引数で受け取る][]
4. [API実装パターン決定][]
    1. [実行性と保守性のトレードオフ][]
    2. [2つのコードを用意する][]
        1.[実行性を優先した実装の選出][]
        2. [保守性を優先した実装の選出][]
5.[提供形態とビルド戦略][]
    1. [提供ファイル網羅][]
        1. [24パターンJS][]
        2. [CSS(code/min)][]
        3. [TS(バンドル)][]
    2. [提供形態の観点][]
        1. [実行環境(bun,node,browser)][]
        2. [モジュール(esm,iife)][]
        3. [ログ出力是非][]
        4. [ミニファイ是非][]
    3. [ビルド方法とその実装][]
        1. [build.shによる実装][]
        2. [bunのASTによるコード調整][]
        3. [実行性と保守性におけるコード切替][]

[割注における分割位置決定方法]:#p1-1
[フォールバックのアルゴリズム]:#p1-2
[責任の混在]:#p1-3
[責任分離とトレードオフ]:#p2
[長所]:#p2-1
[短所]:#p2-2

[API実装パターン網羅]:#p3
[戻り値のパターン]:#p3-1
[分割後テキスト]:#p3-1-1
[`[line1, line2]`]:#p3-1-1-1
[分割位置(index,skip)]:#p3-1-2
[`[index, skip]`]:#p3-1-2-1
[`{index, skip}`]:#p3-1-2-2
[メソッドのパターン]:#p3-2
[命令型パターン]:#p3-2-1
[ループなし]:#p3-2-1-1
[skipハードコーディング]:#p3-2-1-1-1
[skip代入1回のみ]:#p3-2-1-1-2
[分割メタ文字字数カウント]:#p3-2-1-1-3
[ループあり]:#p3-2-1-2
[`if-else if-else`]:#p3-2-1-2-1
[全件走査・例外あり]:#p3-2-1-2-2
[最後のみ強制実行]:#p3-2-1-2-3
[関数配列による抽象化パターン（fallback関数）]:#p3-2-2
[JS型]:#p3-2-2-1
[TS型]:#p3-2-2-2
[責任混在型]:#p3-2-2-3
[ラッパー型]:#p3-2-2-4
[委譲型]:#p3-2-2-5
[Strategiesクラスパターン]:#p3-2-3
[引数受付（run(i)方式）]:#p3-2-3-1
[イテレータ（next()方式）]:#p3-2-3-2
[抽象化パターン]:#p3-2-4
[親クラスによる分類]:#p3-2-4-1
[メソッドチェーン]:#p3-2-4-2
[具象化パターン（WarichuSplitter）]:#p3-2-5
[nullで失格を表現する]:#p3-2-5-1
[Symbolで失格を表現する]:#p3-2-5-2
[オプションをコンストラクタ引数で受け取る]:#p3-2-5-3

[実行性と保守性のトレードオフ]:#p4-1
[2つのコードを用意する]:#p4-2
[実行性を優先した実装の選出]:#p4-2-1
[保守性を優先した実装の選出]:#p4-2-2
[提供形態とビルド戦略]:#p5
[提供ファイル網羅]:#p5-1
[24パターンJS]:#p5-1-1
[CSS(code/min)]:#p5-1-2
[TS(バンドル)]:#p5-1-3
[提供形態の観点]:#p5-2
[実行環境(bun,node,browser)]:#p5-2-1
[モジュール(esm,iife)]:#p5-2-2
[ログ出力是非]:#p5-2-3
[ミニファイ是非]:#p5-2-4[ビルド方法とその実装]:#p5-3
[build.shによる実装]:#p5-3-1
[bunのASTによるコード調整]:#p5-3-2
[実行性と保守性におけるコード切替]:#p5-3-3

<a id="p1"></a>
## 1. [背景と課題][]

1. [割注における分割位置決定方法][]
2. [フォールバックのアルゴリズム][]
3. [責任の混在][]

<a id="p1-1"></a>
### 1.1 [割注における分割位置決定方法][]

割注は本文より小さな字で二行に渡り説明を表示する概念だ。これを実現するにあたり、二行に分割する必要がある。この時、分割位置は次のいずれかで決定される。

No|概要|詳細
--|----|----
1|指定位置|`｜`など任意メタ文字によりユーザが分割位置を明示する
2|単語境界|[Intl.Segmenter][]により単語境界で分割する
3|中間位置|割注テキストを等分する

[Intl.Segmenter]:https://developer.mozilla.org/ja/docs/Web/JavaScript/Reference/Global_Objects/Intl/Segmenter

各方法はオプションにて詳細設定できるが割愛する。

ここで重要なのは、1,2,3の順に位置を取得できるか試行し、失敗すれば次の方法を試すことだ。そして最後は必ず3の中間位置で確定する。

このアプローチはフォールバックの概念に似ている。最後の方法だけは確実に実行されると保証されていることが重要だ。

<a id="p1-2"></a>
### 1.2 [フォールバックのアルゴリズム][]

フォールバックのアルゴリズムについて考察する。以下の工程を経て徐々に改善していく。

1. [`if-else if-else`][]
2. [関数配列][]
3. [最後の関数だけは確実に実行する][]

[`if-else if-else`]:#p1-2-1
[関数配列]:#p1-2-2
[最後の関数だけは確実に実行する]:#p1-2-3

<a id="p1-2-1"></a>
#### 1.2.1. [`if-else if-else`][]

No|概要|API
--|----|----
1|指定位置|`getExplicitSplitIndex`
2|単語境界|`getWordSplitIndex`
3|中間位置|`getDefaultSplitIndex`

```typescript
// 命令型による実装例
for (let i = 0; i < 3; i++) {
  let index: number | null = null;
  let skip = 0;

  // 戦略ごとの実行と合否判定が混在している
  if (i === 0) {
    index = this.getExplicitSplitIndex(graphemes, splitChar);
    skip = 1;
  } else if (i === 1) {
    index = this.getWordSplitIndex(content, totalLen);
  } else {
    index = this.getDefaultSplitIndex(totalLen);
  }

  // 成功した時点で結果を加工して返却する
  if (index !== null) {
    return [
      graphemes.slice(0, index).join(''),
      graphemes.slice(index + skip).join('')
    ];
  }
}
// 最後の戦略まで失敗した場合の処理（論理的に到達不能）
throw new UnreachableError('すべての分割戦略が失敗しました。');
```

このコードの問題は`if-else if-else`が使われていることだ。もし分割位置の方法が増減したら、`if-else if-else`という文まで書き換えねばならない。これはコードの管理コストが高い。

分割位置が増減する時は、分割メソッドを書き換えるだけで済ませたい。

<a id="p1-2-2"></a>
#### 1.2.2. [関数配列][]

分割位置計算する関数を`strategies`という配列にまとめた。

分割位置計算は数ある戦略の内の一つである。もし戦略が増減したら配列内を書き換えるだけで済む。`for`内のアルゴリズムを書き換えずに済む。

```typescript
const strategies = [
  ()=>this.getExplicitSplitIndex(graphemes, splitChar),
  ()=>this.getWordSplitIndex(content, totalLen),
  ()=>this.getDefaultSplitIndex(totalLen)
];
for (let i = 0; i < strategies.length; i++) {
  const index = strategies[i]();
  
  // 成功判定と中断のロジックがビジネスロジックと混在している
  if (index !== null) {
    return [
      graphemes.slice(0, index).join(''),
      graphemes.slice(index + skip).join('')
    ];
  }
}
// 最後の戦略まで失敗した場合の処理（論理的に到達不能）
throw new UnreachableError('すべての分割戦略が失敗しました。');
```

このコードの問題は、最終行`UnreachableError`である。ここに到達することは論理上ありえない。フォールバックとは最後の戦略だけは必ず実行されるものだから、それ以降のコードである`UnreachableError`が実行されることはありえない。

では、なぜありえないのにコードとして書いているのか？　それは最後の戦略が`if (index !== null) {`の条件に合致する保証ができないからだ。戦略を変更した時「最後の結果を返す」ということを保証するには、`strategies`の順番と、その中の関数の実装に依存してしまう。

最後の戦略は`getDefaultSplitIndex`であり、これは`null`を返さない。そのように実装している。しかしその実装を変えてしまえば保証できない。また、仮にそれが実装されていても`getDefaultSplitIndex`の順序が最後でなく最後から二番目になれば、保証できなくなる。これは新しく戦略を追加した時に十分発生しうる事態だ。

一々そんなことを考えて`strategies`を調整せねばならないなど面倒極まりない。

<a id="p1-2-3"></a>
#### 1.2.3. [最後の関数だけは確実に実行する][]

そこで、最後の関数だけは条件式を通さず、必ず実行するようにする。

```typescript
const strategies = [
  ()=>this.getExplicitSplitIndex(graphemes, splitChar),
  ()=>this.getWordSplitIndex(content, totalLen),
  ()=>this.getDefaultSplitIndex(totalLen)
];

if (strategies.length === 0) throw new Error("strategiesは一つ以上必要です。");

// 最後の一つ前の戦略まで実行する
const LAST_IDX = strategies.length - 1;
for (let i = 0; i < LAST_IDX; i++) {
  const index = strategies[i]();
  
  // 成功判定と中断のロジックがビジネスロジックと混在している
  if (index !== null) {
    return [
      graphemes.slice(0, index).join(''),
      graphemes.slice(index + skip).join('')
    ];
  }
}

// 最後の戦略だけ実行する
const index = strategies[LAST_IDX]();
return [
  graphemes.slice(0, index).join(''),
  graphemes.slice(index + skip).join('')
];
```

ポイントは`for`文において最後の一つ前までしか実行しないことだ。そして最後は`for`内`if`を介さずに実行する。これにて最後の戦略だけは絶対に実行するフォールバック方式が実現する。

到達不能なコードという矛盾した記述をせずに済む。

このコードの問題はコードが重複することだ。関数の実行と返却値の作成が重複せぬよう[DRY][]に書きたい。

[DRY]:https://ja.wikipedia.org/wiki/Don%27t_repeat_yourself

<a id="p1-3"></a>
### 1.3 [責任の混在][]

そもそも割注を実装するメソッドの中に、フォールバックを実装するコードを書くことが間違っている。

[ソフトウェア工学][]によれば[関心の分離][]と[単一責任の原則][]に従うべきだ。

No|[ソフトウェア工学][]|概要
--|---------------------|----
1|[関心の分離][]|コードは役割や責任ごとに分割すべきである
2|[単一責任の原則][]|分割した単位は、それぞれ一つの役割・責任だけを持つべきである。

[ソフトウェア工学]:https://ja.wikipedia.org/wiki/%E3%82%BD%E3%83%95%E3%83%88%E3%82%A6%E3%82%A7%E3%82%A2%E5%B7%A5%E5%AD%A6
[関心の分離]:https://ja.wikipedia.org/wiki/%E9%96%A2%E5%BF%83%E3%81%AE%E5%88%86%E9%9B%A2
[単一責任の原則]:https://ja.wikipedia.org/wiki/%E5%8D%98%E4%B8%80%E8%B2%AC%E4%BB%BB%E3%81%AE%E5%8E%9F%E5%89%87

1. 一つの関数やメソッドの中には、一つの役割・責任だけを持ったコードを書くべきである
2. 一つのクラスやモジュールはその上位概念としてメソッドを分類すべきである

一つの関数やメソッドの中に、二つ以上の異なる役割・責任を持たせると、コードは理解不能・修正不能・保守不能になってしまう。

今回、割注の中でフォールバックの仕組みを実装することで、割注の関数は、割注の実装とフォールバックの実装という異なる役割・責任を持った状態になってしまう。これはコードが保守不能になる前兆だ。

<a id="p2"></a>
## 2. [責任分離とトレードオフ][]

コードは責任分離すべきである。この時、その長所と短所がある。

1. [長所](#p2-1)
    1. [ビジネスロジックの純粋化](#p2-1-1)
    2. [変更の局所化](#p2-1-2)
    3. [テスト容易性の向上](#p2-1-3)
2. [短所](#p2-2)
    1. [抽象化による実行オーバーヘッド](#p2-2-1)
    2. [ファイルサイズの増加](#p2-2-2)
    3. [学習コストとブラックボックス化](#p2-2-3)

[長所]:#p2-1
[短所]:#p2-2

<a id="p2-1"></a>
### 2.1 [長所][]

1. [ビジネスロジックの純粋化](#p2-1-1)
2. [変更の局所化](#p2-1-2)
3. [テスト容易性の向上](#p2-1-3)

[ビジネスロジックの純粋化]:#p2-1-1
[変更の局所化]:#p2-1-2
[テスト容易性の向上]:#p2-1-3

<a id="p2-1-1"></a>
#### 2.1.1. [ビジネスロジックの純粋化][]

「分割位置を算出する」という本来の目的（ビジネスロジック）と、「フォールバックの制御」という手段（制御ロジック）が分離される。これにより、ビジネスロジックのコードがノイズから解放され、本来の意図が明確になる。

<a id="p2-1-2"></a>
#### 2.1.2. [変更の局所化][]

フォールバックの仕様変更（例：全失格時の挙動変更）が、割注のビジネスロジックに波及しなくなる。制御ロジックは `fallback` 関数内に閉じ込められているため、修正箇所が明確であり、誤ってビジネスロジックを破壊するリスクが低減する。

<a id="p2-1-3"></a>
#### 2.1.3. [テスト容易性の向上][]

フォールバックの制御ロジックを単体でテスト可能になる。また、ビジネスロジック側は「フォールバックが正しく動くこと」を前提にテストできるため、テストの責務が明確になる。

<a id="p2-2"></a>
## 2.2 [短所][]

1. [抽象化による実行オーバーヘッド](#p2-2-1)
2. [ファイルサイズの増加](#p2-2-2)
3. [学習コストとブラックボックス化](#p2-2-3)

[抽象化による実行オーバーヘッド]:#p2-2-1
[ファイルサイズの増加]:#p2-2-2
[学習コストとブラックボックス化]:#p2-2-3

<a id="p2-2-1"></a>
### 2.2.1. [抽象化による実行オーバーヘッド][]

汎用的な制御フローを抽象化することで、関数呼び出しの回数が増加する。また、戦略を配列や無名関数として渡す構造上、クロージャの生成や動的な関数呼び出しが発生し、直接的な `for` ループや `if` 文によるベタ書きと比較して、実行速度やメモリ消費の面でオーバーヘッドが生じる。

<a id="p2-2-2"></a>
### 2.2.2. [ファイルサイズの増加][]

制御フローを抽象化するためのライブラリコードがバンドルされるため、最終的な出力ファイルのサイズが増加する。特に、小規模なプロジェクトや、極限までファイルサイズを削る必要がある環境においては、このオーバーヘッドが無視できないコストとなる。

<a id="p2-2-3"></a>
### 2.2.3. [学習コストとブラックボックス化][]

独自の抽象化API（`fallback` など）を導入することで、開発者はそのAPIの仕様を学習する必要がある。また、制御フローがライブラリ内部に隠蔽されるため、デバッグ時にスタックトレースが深くなり、問題の発生箇所を直感的に特定することが難しくなる場合がある。

## 3. [API実装パターン網羅][]

1. [戻り値のパターン][]: 3通り
2. [メソッドのパターン][]: 19通り

<a id="p3-1"></a>
### 3.1 [戻り値のパターン][]

戻り値は[二つのテキストに分割した文字列の配列][分割後テキスト]になる。

だがこれを採用すると、文字列を二分割するコードを重複して実装せねばならなくなる。そこで「[分割位置（インデックスとスキップ数）を返す方法][分割位置(index,skip)]」で実装することにした。しかしそのせいで`skip`というメタ文字分割方法でしか使わない値を、他の方法でも`0`として返さねばならなくなってしまった。

戻り値のパターンは大きく分けて「[分割後のテキストを直接返す方法][分割後テキスト]」と「[分割前の位置情報（インデックスとスキップ数）を返す方法][分割位置(index,skip)]」の2つがあり、さらに後者は[配列][`[index, skip]`]か[オブジェクト][`{index, skip}`]かで分かれる。

本ドキュメントの以降の章（3.2 [メソッドのパターン][]）では、単一責任の原則、保守性（DRY原則）、可読性の観点から、[オブジェクト][`{index, skip}`][]パターンを最終的に採用する。

<a id="p3-1-1"></a>
#### 3.1.1 [分割後テキスト][]

<a id="p3-1-1-1"></a>
##### 3.1.1.1 [`[line1, line2]`][]

割注テキストを二行で分割した文字列配列を返す。

```typescript
// 戦略関数の戻り値の例
return[
  graphemes.slice(0, index!).join(''),
  graphemes.slice(index!).join('')
];
```

**問題点**:
この形式を各戦略関数に実装すると、文字列を `slice` して `join` するという「出力結果の生成ロジック」が、すべての戦略関数内で重複してしまう。これはDRY原則に反し、出力形式を変更する際にすべての戦略関数を修正しなければならないため、保守性が低い。

<a id="p3-1-2"></a>
#### 3.1.2 [分割位置(index,skip)][]

割注テキストを二行で分割するための分割位置を算出する。

**問題点**:
分割位置は`index`と`skip`の二値を要する。本来であればindexのみで良かったが、メタ文字で分割位置指定する方法が存在するせいで、メタ文字文だけ`skip`する必要がある。よって`skip`の概念が不要な他の方法までもが`skip`値を`0`として返却せねばならない。

<a id="p3-1-2-1"></a>
##### 3.1.2.1 [`[index, skip]`][]

位置情報を配列で返すパターン。

```typescript
// 戦略関数の戻り値の例
return [index, skip];

// 呼び出し側の利用例
const res = strategy();
const line1 = graphemes.slice(0, res[0]).join('');
const line2 = graphemes.slice(res[0] + res[1]).join('');
```

**問題点**:
呼び出し側のコードにおいて、`res[0]` や `res[1]` といったマジックナンバー（インデックス番号）でアクセスすることになる。これが何を意味する値なのかが直感的に分かりづらく、可読性が低い。

<a id="p3-1-2-2"></a>
##### 3.1.2.2[`{index, skip}`]

位置情報をオブジェクトで返すパターン。

```typescript
// 戦略関数の戻り値の例
return { index, skip };

// 呼び出し側の利用例
const res = strategy();
const line1 = graphemes.slice(0, res.index).join('');
const line2 = graphemes.slice(res.index + res.skip).join('');
```

**長所**:
呼び出し側のコードにおいて、`res.index` や `res.skip` といったプロパティ名でアクセスできるため、その値が何を意味しているのかが一目で分かる。配列アクセス（`res[0]`）のようなマジックナンバーを排除でき、可読性が最も高い。

**短所**:
オブジェクトを生成する分、配列を返すよりもわずかにメモリと実行コストがかかる。繰り返すたびそのコストは嵩む。

**結論**:
私の戦略として、実行コストの削減よりもコードの保守性と可読性の向上を優先することを決定した。よって本ドキュメントではこの形式を採用する。

<a id="p3-2"></a>
### 3.2. [メソッドのパターン][]

<a id="p3-2-1"></a>
#### 3.2.1. [命令型パターン][]

<a id="p3-2-1-1"></a>
##### 3.2.1.1. [ループなし][]

<a id="p3-2-1-1-1"></a>
###### 3.2.1.1.1 [skipハードコーディング][]

割注テキストを分割する3つの方法をそのままベタ書きした。責任の混在と`skip`の必要によりコードが複雑化した。

このコードは責任が混在している。「分割位置の算出(順序決定、取捨選択、位置決定)」と「二分割文字列の作成」という異なる複数の責任が一つのコードに混在している。

このコードは`skip`が必要だ。複数の異なる戦略(分割位置算出方法)を実行しつつ、出力結果は共通した方法で実装している。このため特定の戦略でしか使わないはずの`skip`が全体を通して必要になってしまう。

本来`skip`は指定位置分割`getExplicitSplitIndex`でのみ必要な値だ。メタ文字の数だけ位置をスキップすることで、出力する文字列を決定するための値である。しかし`skip`が出力式に含まれているため、他の方法では`skip`が不要であることを`0`で示す必要が出てくる。これにより重複コードを書かねばならない。

```typescript
let index = this.getExplicitSplitIndex(graphemes, splitChar);
let skip = 1; // 指定位置分割の場合はメタ文字分をスキップ

if (index === null) {
  index = this.getWordSplitIndex(content, totalLen);
  skip = 0; // 単語分割の場合はスキップ不要
}

if (index === null) {
  index = this.getDefaultSplitIndex(totalLen);
  skip = 0; // 中間位置分割の場合もスキップ不要
}

return[
  graphemes.slice(0, index!).join(''),
  graphemes.slice(index! + skip).join('')
];
```

**長所**:
*   実行が速い。関数呼び出しや複雑な計算が発生しないため、実行速度が速い。

**短所**:
*   コードの保守性が低い。「分割位置の算出」と「二分割文字列の作成」という異なる二つの責任が混在しているせい。
*   コードの修正コストが高い。戦略の増減を検討するたびに`if`文や`skip = 0` のようなロジックの妥当性検証まで必要になる。
*   コードが重複する。`if`や`skip = 0`など同一であるべき箇所の実装が戦略の数だけ重複してしまう。DRYの原則に従い一つのコードで実装すべきだが責任の混在により不可能。

<a id="p3-2-1-1-2"></a>
###### 3.2.1.1.2[skip代入1回のみ][]

`skip` の代入を初期化時の1回のみに制限し、重複コードを削減したパターン。

前項の[skipハードコーディング][]では、不要なはずの `skip = 0` を各戦略の分岐内に記述しなければならないというDRY原則違反があった。これを解決するため、最初の戦略（指定位置分割）が失敗した時点で `skip` に `0` を代入し、以降の分岐では `skip` に触れない構造とした。これは`skip`が必要なのは最初の戦略だけである事実とも合致し、可読性の向上にもつながる。

```typescript
// 命令型による実装例（skip代入1回のみ）
let index = this.getExplicitSplitIndex(graphemes, splitChar);
let skip = index === null ? 0 : 1; // 失敗なら0、成功ならメタ文字分(1)をセット

if (index === null) {
  index = this.getWordSplitIndex(content, totalLen);
}

if (index === null) {
  index = this.getDefaultSplitIndex(totalLen);
}

return[
  graphemes.slice(0, index!).join(''),
  graphemes.slice(index! + skip).join('')
];
```

**長所**:
*   コードの重複が減る。`skip = 0` という不要な代入文を各分岐から排除できたため、前項よりも記述量が減り、DRY原則に近づいた。

**短所**:
*   実行コストが増える。[skipハードコーディング][]より条件分岐処理が一つ増えてしまう。
*   責任の混在は解決していない。「分割位置の算出」と「二分割文字列の作成（`skip` の管理）」が同一関数内に混在しているという根本的な問題は残ったままであり、保守性の低さは改善されていない。























<a id="p3"></a>
## 3. [API設計][]

13通りを考察した。

1. [命令型パターン](#p3-1)
    1. [ループなし](#p3-1-1)
        1. [関数を直接順番に呼び出す](#p3-1-1-1)
    2. [ループあり](#p3-1-2)
        1. [`if-else if-else`](#p3-1-2-1)
        2. [全件走査・例外あり](#p3-1-2-2)
        3. [最後のみ強制実行](#p3-1-2-3)
2. [関数配列による抽象化パターン（fallback関数）](#p3-2)
    1. [JS型](#p3-2-1)
    2. [TS型](#p3-2-2)
    3. [責任混在型](#p3-2-3)
    4. [ラッパー型](#p3-2-4)
    5. [委譲型](#p3-2-5)
3. [Strategiesクラスパターン](#p3-3)
    1. [引数受付（run(i)方式）](#p3-3-1)
    2. [イテレータ（next()方式）](#p3-3-2)
4. [抽象化パターン](#p3-4)
    1. [親クラスによる分類](#p3-4-1)
    2. [メソッドチェーン](#p3-4-2)
5. [具象化パターン](#p3-5)
    1. [nullで失格を表現する](#p3-5-1)
    2. [Symbolで失格を表現する](#p3-5-2)

<a id="p3-1"></a>
## 3.1 [命令型パターン][]

1. [ループなし](#p3-1-1)
2. [ループあり](#p3-1-2)

[ループなし]:#p3-1-1
[ループあり]:#p3-1-2

<a id="p3-1-1"></a>
### 3.1.1 [ループなし][]

1. [関数を直接順番に呼び出す](#p3-1-1-1)

[関数を直接順番に呼び出す]:#p3-1-1-1

<a id="p3-1-1-1"></a>
#### 3.1.1.1 [関数を直接順番に呼び出す][]

順次実行し、成功すれば中断する。

```typescript
// 命令型による実装例（ループなし）
let index = this.getExplicitSplitIndex(graphemes, splitChar);
let skip = null===index ? 0 : 1;
if (index === null) {index = this.getWordSplitIndex(content, totalLen)}
if (index === null) {index = this.getDefaultSplitIndex(totalLen)}
return [
  graphemes.slice(0, index!).join(''),
  graphemes.slice(index! + skip).join('')
];
```


```typescript
// 命令型による実装例（ループなし）
let index = this.getExplicitSplitIndex(graphemes, splitChar);
let skip = 1;

if (index === null) {
  index = this.getWordSplitIndex(content, totalLen);
  skip = 0;
}

if (index === null) {
  index = this.getDefaultSplitIndex(totalLen);
  skip = 0;
}

return [
  graphemes.slice(0, index!).join(''),
  graphemes.slice(index! + skip).join('')
];
```

**問題点**:
戦略が増減するたびに、中断条件（`if (index === null)`）の記述が重複し、コードの保守性が著しく低下する。また、各戦略の実行順序がハードコーディングされており、順序の入れ替えや戦略の追加が困難である。

<a id="p3-2"></a>
### 3.2. [メソッドのパターン][]

<a id="p3-2-1"></a>
#### 3.2.1. [命令型パターン][]

<a id="p3-2-1-1"></a>
##### 3.2.1.1. [ループなし][]

<a id="p3-2-1-1-1"></a>
###### 3.2.1.1.1 [skipハードコーディング][]

割注テキストを分割する3つの方法をそのままベタ書きした。責任の混在と`skip`の必要によりコードが複雑化した。

このコードは責任が混在している。「分割位置の算出(順序決定、取捨選択、位置決定)」と「二分割文字列の作成」という異なる複数の責任が一つのコードに混在している。

このコードは`skip`が必要だ。複数の異なる戦略(分割位置算出方法)を実行しつつ、出力結果は共通した方法で実装している。このため特定の戦略でしか使わないはずの`skip`が全体を通して必要になってしまう。

本来`skip`は指定位置分割`getExplicitSplitIndex`でのみ必要な値だ。メタ文字の数だけ位置をスキップすることで、出力する文字列を決定するための値である。しかし`skip`が出力式に含まれているため、他の方法では`skip`が不要であることを`0`で示す必要が出てくる。これにより重複コードを書かねばならない。

```typescript
let index = this.getExplicitSplitIndex(graphemes, splitChar);
let skip = 1; // 指定位置分割の場合はメタ文字分をスキップ

if (index === null) {
  index = this.getWordSplitIndex(content, totalLen);
  skip = 0; // 単語分割の場合はスキップ不要
}

if (index === null) {
  index = this.getDefaultSplitIndex(totalLen);
  skip = 0; // 中間位置分割の場合もスキップ不要
}

return[
  graphemes.slice(0, index!).join(''),
  graphemes.slice(index! + skip).join('')
];
```
**長所**:
    *   実行が速い。関数呼び出しや複雑な計算が発生しないため、実行速度が速い。

**短所**:
    *   コードの保守性が低い。「分割位置の算出」と「二分割文字列の作成」という異なる二つの責任が混在しているせい。
    *   コードの修正コストが高い。戦略の増減を検討するたびに`if`文や`skip = 0` のようなロジックの妥当性検証まで必要になる。
    *   コードが重複する。`if`や`skip = 0`など同一であるべき箇所の実装が戦略の数だけ重複してしまう。DRYの原則に従い一つのコードで実装すべきだが責任の混在により不可能。

<a id="p3-2-1-1-2"></a>
###### 3.2.1.1.2[skip代入1回のみ][]

`skip` の代入を初期化時の1回のみに制限し、重複コードを削減したパターン。

前項の[skipハードコーディング][]では、不要なはずの `skip = 0` を各戦略の分岐内に記述しなければならないというDRY原則違反があった。これを解決するため、最初の戦略（指定位置分割）が失敗した時点で `skip` を `0` に決定し、以降の分岐では `skip` に触れない構造とした。

もとより`skip`は最初の分割方法`getExplicitSplitIndex`が有効値を返した場合のみ`1`にすべき値だ。それ以外は不要であり`0`とすべき値である。その意味において、不要な他の分割方法で代入することを辞めたのは可読性が向上する。

```typescript
// 命令型による実装例（skip代入1回のみ）
let index = this.getExplicitSplitIndex(graphemes, splitChar);
let skip = index === null ? 0 : 1; // 失敗なら0、成功ならメタ文字分(1)をセット

if (index === null) {
  index = this.getWordSplitIndex(content, totalLen);
}

if (index === null) {
  index = this.getDefaultSplitIndex(totalLen);
}

return[
  graphemes.slice(0, index!).join(''),
  graphemes.slice(index! + skip).join('')
];
```

**長所**:
*   コードの重複が減る。`skip = 0` という不要な代入文を各分岐から排除できたため、前項よりも記述量が減り、DRY原則に近づいた。

**短所**:
*   実行パスが増えた。3.2.1.1.1 [skipハードコーディング][]の方法より`if`文が一つ増えて処理量が増えた。
*   責任の混在は解決していない。「分割位置の算出」と「二分割文字列の作成（`skip` の管理）」が同一関数内に混在しているという根本的な問題は残ったままであり、保守性の低さは改善されていない。


<a id="p4"></a>
## 4. [API実装パターン決定][]

<a id="p4-1"></a>
### 4.1 [実行性と保守性のトレードオフ][]

<a id="p4-2"></a>
### 4.2 [2つのコードを用意する][]

<a id="p4-2-1"></a>
#### 4.2.1 [実行性を優先した実装の選出][]

<a id="p4-2-2"></a>
#### 4.2.2 [保守性を優先した実装の選出][]

<a id="p5"></a>
## 5. [提供形態とビルド戦略][]

<a id="p5-1"></a>
### 5.1 [提供ファイル網羅][]

<a id="p5-1-1"></a>
#### 5.1.1 [24パターンJS][]

<a id="p5-1-2"></a>
#### 5.1.2 [CSS(code/min)][]

<a id="p5-1-3"></a>
#### 5.1.3 [TS(バンドル)][]

<a id="p5-2"></a>
### 5.2 [提供形態の観点][]

<a id="p5-2-1"></a>
#### 5.2.1 [実行環境(bun,node,browser)][]

<a id="p5-2-2"></a>
#### 5.2.2 [モジュール(esm,iife)][]

<a id="p5-2-3"></a>
#### 5.2.3 [ログ出力是非][]

<a id="p5-2-4"></a>
#### 5.2.4 [ミニファイ是非][]

<a id="p5-3"></a>
### 5.3 [ビルド方法とその実装][]

<a id="p5-3-1"></a>
#### 5.3.1 [build.shによる実装][]

<a id="p5-3-2"></a>
#### 5.3.2 [bunのASTによるコード調整][]

<a id="p5-3-3"></a>
#### 5.3.3 [実行性と保守性におけるコード切替][]

